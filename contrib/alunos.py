# encoding: utf-8
# Obtido com o comando:
#
# >>> from pprint import pprint
# >>> from app.subscription.models import Susbcription
# >>> pprint(list(Subscription.objects.all().values()))

import datetime
alunos = []
